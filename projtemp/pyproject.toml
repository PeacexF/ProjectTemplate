[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "projtemp"
version = "0.1.0"
description = "Scatter a new project from the ProjectTemplate starting points."
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["click>=8.1"]

[project.scripts]
projtemp = "projtemp:main"

[tool.hatch.build.targets.wheel]
only-include = ["projtemp.py"]
